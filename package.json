{
  "name": "wikio",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch & npx wrangler pages dev dist --d1 DB=wikio",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler pages deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
